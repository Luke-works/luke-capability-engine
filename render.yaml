# ARCHIVED — luke-capability-engine has been MERGED into luke-core-engine (2026-06).
#
# Its domain (capabilities, subscriptions, grants, forms, email, secrets, embed)
# now runs IN-PROCESS inside luke-core-engine: Camunda owns orchestration and these
# tables are the data store. There is no separate capability service to deploy.
#
# Action: disconnect this repo's Render Blueprint. This file intentionally defines
# NO services so the old blueprint deploys nothing.
#
# See luke-platform/docs/MERGE_MASTER_PROMPT.md for the merge.
